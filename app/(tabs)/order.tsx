import { View, Text } from "react-native";
import React from "react";

const Order = () => {
  return (
    <View className="flex justify-center items-center h-full">
      <Text>This is the Order session</Text>
    </View>
  );
};

export default Order;
